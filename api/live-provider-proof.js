const crypto=require("node:crypto");
const REPO="pjmcveyroutalk/routalk-pilot";const CONFIRMATION="LIVE_PROVIDER_PROOF_ONCE";
function safeEqual(l,r){const a=Buffer.from(l||"");const b=Buffer.from(r||"");return a.length===b.length&&crypto.timingSafeEqual(a,b)}
function bearer(req){const v=req.headers?.authorization||"";return v.startsWith("Bearer ")?v.slice(7):""}
async function vercelFetch(path,init={}){const token=process.env.VERCEL_TOKEN;if(!token)throw new Error("missing_vercel_token");const r=await fetch(`https://api.vercel.com${path}`,{...init,headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json",...(init.headers||{})},cache:"no-store"});const text=await r.text();let body={};try{body=text?JSON.parse(text):{}}catch{body={raw:text}}if(!r.ok){const e=new Error(`vercel_http_${r.status}`);e.status=r.status;e.body=body;throw e}return body}
async function githubMainSha(){const headers={Accept:"application/vnd.github+json","User-Agent":"routalk-pilot-live-provider-proof"};const token=process.env.GITHUB_TOKEN||process.env.PILOT_GITHUB_TOKEN||"";if(token)headers.Authorization=`Bearer ${token}`;const r=await fetch(`https://api.github.com/repos/${REPO}/commits/main`,{headers,cache:"no-store"});if(!r.ok)throw new Error(`github_main_sha_http_${r.status}`);const b=await r.json();if(!b?.sha||!/^[0-9a-f]{40}$/i.test(b.sha))throw new Error("invalid_github_main_sha");return b.sha}
async function getProject(){const id=process.env.VERCEL_PROJECT_ID;if(!id)throw new Error("missing_vercel_project_id");return vercelFetch(`/v9/projects/${encodeURIComponent(id)}`)}
async function waitForDeployment(id,timeoutMs=120000){const started=Date.now();while(Date.now()-started<timeoutMs){const d=await vercelFetch(`/v13/deployments/${encodeURIComponent(id)}`);const s=d.readyState||d.status;if(s==="READY")return d;if(s==="ERROR"||s==="CANCELED")throw new Error(`deployment_${String(s).toLowerCase()}`);await new Promise(r=>setTimeout(r,2500))}throw new Error("deployment_wait_timeout")}
function createVercelClient(project){return{async deploy(request){const repoId=project?.link?.repoId;if(!repoId)throw new Error("vercel_project_missing_github_repo_id");const payload={name:project.name||"routalk-pilot",project:process.env.VERCEL_PROJECT_ID,target:"production",gitSource:{type:"github",repoId,ref:"main",sha:request.sourceRevision},meta:{pilotLiveProviderProof:request.releaseCandidateId,pilotSourceRevision:request.sourceRevision}};const created=await vercelFetch("/v13/deployments",{method:"POST",body:JSON.stringify(payload)});const deploymentId=created.id||created.uid;if(!deploymentId)throw new Error("vercel_deployment_missing_id");const ready=await waitForDeployment(deploymentId);return{deploymentId,status:"DEPLOYED",evidence:{providerReadyState:ready.readyState||ready.status||null,createdAt:ready.createdAt||null}}}}}
async function observeProduction({deploymentId,expectedRevision}){const dep=await vercelFetch(`/v13/deployments/${encodeURIComponent(deploymentId)}`);const observedRevision=dep?.meta?.githubCommitSha||dep?.gitSource?.sha||dep?.meta?.pilotSourceRevision||null;const configured=process.env.PILOT_PRODUCTION_URL||process.env.PRODUCTION_URL||process.env.VERCEL_PROJECT_PRODUCTION_URL||"https://routalk-pilot.vercel.app";const target=/^https?:\/\//i.test(configured)?configured:`https://${configured}`;const c=new AbortController();const timeout=setTimeout(()=>c.abort(),10000);let healthStatus="FAILED",httpStatus=null;try{const r=await fetch(target.replace(/\/+$/,"")+"/",{method:"GET",redirect:"follow",cache:"no-store",signal:c.signal});httpStatus=r.status;const body=await r.text();if(r.ok&&body.includes("<title>Routalk Pilot</title>")&&body.includes("Routalk Pilot"))healthStatus="PASSED"}finally{clearTimeout(timeout)}return{observedRevision,expectedRevision,healthChecks:[{id:"canonical-production-root",required:true,status:healthStatus,httpStatus}]}}
module.exports=async function handler(req,res){const requestId=crypto.randomUUID();res.setHeader("Cache-Control","no-store, max-age=0");res.setHeader("Pragma","no-cache");res.setHeader("X-Content-Type-Options","nosniff");res.setHeader("X-Frame-Options","DENY");res.setHeader("X-Pilot-Request-Id",requestId);if(req.method!=="POST"){res.setHeader("Allow","POST");return res.status(405).json({ok:false,error:"method_not_allowed",requestId})}const secret=process.env.PILOT_TRIGGER_SECRET||"";if(!secret||!safeEqual(bearer(req),secret))return res.status(401).json({ok:false,error:"unauthorized",requestId});if(req.body?.confirmation!==CONFIRMATION||req.body?.approvalIntent!==true)return res.status(400).json({ok:false,error:"explicit_live_proof_confirmation_required",providerInvocations:0,requestId});const required=[process.env.VERCEL_TOKEN,process.env.VERCEL_PROJECT_ID,process.env.PILOT_TRIGGER_SECRET,process.env.PILOT_PRODUCTION_URL||process.env.PRODUCTION_URL||process.env.VERCEL_PROJECT_PRODUCTION_URL];if(!required.every(Boolean))return res.status(503).json({ok:false,error:"live_provider_not_ready",providerInvocations:0,requestId});try{const sourceRevision=await githubMainSha();const project=await getProject();const releaseCandidateId=`pilot-live-proof-${sourceRevision.slice(0,12)}`;const{runGuardedLiveProviderControl}=await import("../pilot-verification/live-provider-proof-control.mjs");const result=await runGuardedLiveProviderControl({confirmation:CONFIRMATION,approvalIntent:true,sourceRevision,releaseCandidateId,approver:"authenticated-phone-operator",packages:[{packageId:`provider-independent-main-${sourceRevision.slice(0,12)}`,verificationStatus:"PASSED"}]},{vercelClient:createVercelClient(project),observeProduction});const verified=result.status==="PRODUCTION_VERIFIED"&&result.providerInvocations===1;return res.status(verified?200:503).json({ok:verified,status:result.status,providerInvocations:result.providerInvocations??0,releaseCandidateId,sourceRevision,deploymentId:result.release?.dispatch?.result?.deploymentId||null,production:result.production||null,requestId})}catch(error){return res.status(500).json({ok:false,error:error?.message||"live_provider_proof_failed",providerInvocations:0,requestId})}};
module.exports._test={safeEqual,bearer};
