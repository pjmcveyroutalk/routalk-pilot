name: ChatGPT Drive Bridge — Retired

"on":
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired bridge
        run: |
          echo "This legacy bridge is retired."
          echo "Routalk Pilot Private Queue is the canonical processor."
