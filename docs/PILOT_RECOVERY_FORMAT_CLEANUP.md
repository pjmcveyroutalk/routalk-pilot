# Recovery Surface Formatting Cleanup

This change restores readable formatting to `resume.html` after the recovery-approval integration.

It is intentionally behavior-preserving: command recovery, ephemeral-secret handling, automatic polling, pull-request links, and protected merge approval all remain unchanged.
